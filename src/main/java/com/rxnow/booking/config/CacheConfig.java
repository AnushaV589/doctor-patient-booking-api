package com.rxnow.booking.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class CacheConfig {
    // Removed caching configuration since we're using in-memory storage
    // Caching is not needed for in-memory data structures
}